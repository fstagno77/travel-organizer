## Codebase Patterns
- tripPage.js usa IIFE `(async function() { ... })()` — i test si basano su code analysis (grep sul sorgente), non su esecuzione DOM
- La modale "Aggiungi prenotazione" è costruita dinamicamente in `showAddBookingModal` con template literal; i test verificano i pattern nel sorgente
- Classi CSS modifier per modal: `.modal--wide` sovrascrive `max-width`, pattern uguale a `.modal--share` e `.modal--pending-booking`
- Per delegare ad un modulo futuro, usare `window.nomModulo.nomeMetodo` con guard `if (window.nomModulo && typeof window.nomModulo.open === 'function')`
- I test Us-xxx usano code analysis con `fs.readFileSync` + `toContain` per verificare struttura HTML generata dinamicamente
- us005-ferry-preview.test.js ha una failure pre-esistente ("Veicoli a bordo") non correlata a US-001
- I test backend (netlify/functions) usano jest.mock su auth, storage, pdfProcessor, tripDates, deduplication, notificationHelper — pattern già consolidato in manage-activity.test.js e delete-passenger.test.js
- delete-passenger.test.js ha failure pre-esistenti (getServiceClient not a function) — non correlate a questo PRD
- Il nuovo branch `action='manual-booking'` va inserito in add-booking.js PRIMA della destructuring `const { pdfs, tripId, ... }` (riga ~321) ma DOPO il branch upload-passenger-pdf
- Campi obbligatori nei required fields per tipo: flight=['date','departureCity','arrivalCity','departureTime','flightNumber','airline'], hotel=['name','city','checkIn.date','checkOut.date']
- manualBookingForm.js pattern: buildFlightForm() restituisce { form, getValues, validate, saveBtn, getFile } — lo stesso pattern va usato per US-004+ (hotel, train, bus, rental, ferry)
- Per ogni tipo di form manuale: aggiungere un branch `if (type === 'X') { formModule = buildXForm(prefill); }` nella funzione `open()` di manualBookingForm.js
- I campi con id `mbf-{campo}` usano `data-required` dataset per il controllo updateSaveBtn — tutti i required devono avere `required: true` in buildField()
- Messaggi di errore con apostrofo nel codice sorgente usano `\'` (es. `dell\'hotel`) — nei test usare `.toContain("dell")` e `.toContain("hotel")` separati, oppure la stringa senza apostrofo
- I form con date relative (check-in/out, ritiro/restituzione) devono calcolare giorni/notti in read-only con `addEventListener('change', ...)` su entrambi i campi data

# Ralph Progress Log
Started: Thu Apr  2 12:25:58 CEST 2026
---

## 2026-04-02 - US-001
- Implementata la griglia 6 card tipo prenotazione nella modale "Aggiungi prenotazione"
- La modale è allargata a 720px tramite classe `modal--wide`
- Aggiunta funzione `showManualBookingForm(type)` con delega a `window.manualBookingForm.open` (per US-003+)
- Aggiunta funzione `bindBookingTypeCardEvents()` per collegare click sulle card
- Card usano colori/icone/gradienti da `activityCategories.js` (coerenza visiva)
- Upload PDF senza card selezionata mantiene comportamento SmartParse invariato
- Files changed: `js/tripPage.js`, `css/components.css`, `tests/us001-manual-booking-step1.test.js`
- **Learnings for future iterations:**
  - `bookingTypes` array è definito inline in `showAddBookingModal` — US-003+ può aggiungere dati in questo array
  - I test verificano il codice sorgente (code analysis), non l'esecuzione DOM — approccio già consolidato nel progetto
  - Il punto di estensione per i form manuali è `window.manualBookingForm.open(type, modal, tripId, { onSaved })`
  - La griglia CSS usa `grid-template-columns: repeat(3, 1fr)` — 3 card per riga, 2 righe per 6 tipi
  - CSS custom properties `--booking-card-color`, `--booking-card-hover-bg`, `--booking-card-gradient` sono impostati inline sulle card
---

## 2026-04-02 - US-002
- Aggiunto branch `action='manual-booking'` in `netlify/functions/add-booking.js`
- Validazione campi obbligatori per tutti i 6 tipi (flight, hotel, train, bus, rental, ferry) con supporto dot notation per campi annidati (es. `checkIn.date`)
- Il dato viene salvato nella collection corretta (flights/hotels/trains/etc.) con id assegnato automaticamente (`{type}-{n}`)
- documentUrl opzionale salvato nel campo `source` del booking
- 9 test creati in `tests/us002-manual-booking-backend.test.js`
- Files changed: `netlify/functions/add-booking.js`, `tests/us002-manual-booking-backend.test.js`
- **Learnings for future iterations:**
  - Il handler add-booking.js verifica `body.action` prima di tutto — aggiungere nuovi branch prima della destructuring `const { pdfs, tripId, ... }`
  - I required fields usano dot notation per campi annidati: `checkIn.date`, `departure.city` ecc. — `getNestedVal()` helper risolve la path
  - Per i test backend: mockare `getServiceClient` nella stessa mock di `utils/auth` (viene importato da lì)
  - Il pattern action-based è consolidato: aggiungere `if (body.action === '...')` in cima all'handler
---

## 2026-04-02 - US-003
- Creato js/manualBookingForm.js con window.manualBookingForm.open(type, modal, tripId, { onSaved })
- Implementato buildFlightForm(prefill) con 6 campi obbligatori + 6 opzionali + CustomSelect per classe
- Validazione inline (showFieldError/clearFieldError), bottone Salva disabilitato fino a required compilati
- Upload documento opzionale con drag-drop (buildDocumentUpload)
- saveManualBooking() chiama add-booking con action='manual-booking'
- Aggiunto import in js/entries/trip.js (prima di tripPage.js per disponibilità window.manualBookingForm)
- 32 test in tests/us003-manual-booking-flight.test.js — tutti passano
- Files changed: js/manualBookingForm.js (nuovo), js/entries/trip.js, tests/us003-manual-booking-flight.test.js (nuovo)
- **Learnings for future iterations:**
  - buildFlightForm() restituisce { form, getValues, validate, saveBtn, getFile } — replicare pattern per US-004+
  - Per aggiungere tipo: aggiungere `if (type === 'X') { formModule = buildXForm(prefill); }` in open()
  - id campi: prefisso `mbf-` + nome campo (es. mbf-hotel-name, mbf-hotel-city, ecc.)
  - buildRow() affianca due form-group con display:flex — utile per tutti i form
  - Il form viene inserito nel modal-body; footer viene svuotato e popolato con Indietro + Salva
---

## 2026-04-02 - US-004
- Creato buildHotelForm(prefill) in js/manualBookingForm.js con 4 campi obbligatori + 9 opzionali
- Campo notti calcolato automaticamente (read-only) da check-in/check-out con calcNights()
- Colazione inclusa usa CustomSelect (no <select> nativo), con fallback input text in env test
- Validazione inline: errore specifico se check-out antecedente check-in
- Upload documento opzionale (riuso buildDocumentUpload)
- Branch `type === 'hotel'` aggiunto in open() di manualBookingForm.js
- Fix catch block in open(): restore del testo originale del save button con saveBtnOriginalText
- 29 test in tests/us004-manual-booking-hotel.test.js — tutti passano
- Files changed: js/manualBookingForm.js, tests/us004-manual-booking-hotel.test.js (nuovo)
- **Learnings for future iterations:**
  - Per i form con date relative (hotel, noleggio): aggiungere funzione helper `calcXxx()` per il calcolo read-only e agganciarla su `addEventListener('change', ...)` su entrambi i campi data
  - Messaggi errore con apostrofo vanno testati con toContain() su substring senza apostrofo
  - Il catch block di open() ora usa `saveBtnOriginalText` per restaurare il testo del bottone — non modificarlo per i prossimi tipi
  - buildHotelForm restituisce { form, getValues, validate, saveBtn, getFile } — pattern identico a buildFlightForm
---
